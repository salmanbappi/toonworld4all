include(":core")
include("src:en:toonworld4all")

project(":core").projectDir = file("../dflix/core")

// Note: In a real multi-extension repo, these would be in the same root
// For this standalone setup, I'm pointing core to the dflix core for reuse
